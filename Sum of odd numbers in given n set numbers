#include<stdio.h>
int main()
{
    int n,i,od=0,num;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&num);
        if(num%2==1)
        {
            od=od+num;
        }
    }
    printf("sum of odd numbers: %d",od);
}
