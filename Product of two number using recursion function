#include<stdio.h>
int pro(int m,int n)
{
    if(n==0)
    {
        return 0;
    }
    else
    {
        return m+pro(m,n-1);
    }
}
int main()
{
    int a,b,p;
    scanf("%d%d",&a,&b);
    p=pro(a,b);
    printf("product: %d",p);
}
