#include<stdio.h>
int main()
{
    int n,i,sum=0,num;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&num);
        if(num%8==0)
        {
            sum=sum+num;
        }
    }
    printf("sum of numbers which are divisible by 8: %d",sum);
}
